# PunisherX Velocity Bridge database configuration
# Set these to point at the same database PunisherX uses so bridge events are visible.
host=localhost
port=3306
database=punisherx
username=root
password=change_me
# Interval in milliseconds for polling bridge_events
poll-interval-ms=1000
