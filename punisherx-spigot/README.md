# PunisherX Spigot

Native Spigot implementation of PunisherX.

The module mirrors the Paper implementation while using the Bukkit scheduler, classic `plugin.yml`
command registration, Spigot login events, and a Libby-based runtime dependency loader. Folia is not
supported by this artifact.
